#include <stdio.h>
/**
 *Description: main - description for task.
 *
 *Return: 0 as a default return value
 */
int main(void)
{
	int letter;

	for (letter = 0; letter <= 9; letter++)
		printf("%d", letter);

	printf("\n");

	return (0);
}
